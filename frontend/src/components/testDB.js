import React from "react"
import logo from '../logo.svg';
import '../App.css';

export const TestDB = () => (
    <div>
    <header className="App-header">
     <img src={logo} className="App-logo" alt="logo" />
     <p>
       DB connected at port 5000.
     </p>
   </header>
 </div>
  )

